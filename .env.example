# Shell environment example. Load explicitly; the application does not parse .env files.
# set -a; source .env; set +a
METROPULSE_PROJECT_ROOT=.
METROPULSE_DB_PATH=data/warehouse/metropulse.duckdb
METROPULSE_API_BASE_URL=http://127.0.0.1:8000
METROPULSE_CORS_ORIGINS=http://127.0.0.1:5173,http://localhost:5173
